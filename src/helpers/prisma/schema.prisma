datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

generator client {
  provider = "prisma-client-js"
}

model Dummy {
  id Int @id @default(autoincrement())
}

model Test {
  id Int @id @default(autoincrement())
}
